# Setup 
```py
    # optional: virtual environment để ko cài packages globally
    py -m venv .venv
    source .venv/bin/activate # linux

    # cài nếu thiếu packages
    pip install -r requirements.txt
```

# Chạy 

```py
    py main.py > log.txt

    # kiểm tra log ở log.txt 
```